# go-api-template

## A template for an authenticated go API

### Create a .env file in the root of the internal directory, containing:

```
SECRET_KEY=<your secret key>
```

### To create a secret key run:

```
openssl rand -hex 32
```
